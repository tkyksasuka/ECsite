<%= image_tag @item.get_profile_image(100,100) %>

<%= @item.name %>

<%= @item.introduction %>

<%= =@item.price*1.1 "(item.price)" %> 税込？

<%= form_with model: item_new do |f| %>
<個数選択プルダウン>
<%= f.submit 'カートに入れる' %>
<% end %>

