<h2>注文情報確認</h2>

<table>
  <thead>
    <tr colspan = "5">
      <th>商品名</th>
      <th>単価(税込)</th>
      <th>数量</th>
      <th>小計</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>
        <%= cart_item.item.name %>
      </td>
      <td>
        <%= cart_item.item.price %>
      </td>
      <td>
        <%= cart_item.amount %>
      </td>
      <td>
        <%= cart_item.item.price*amount %>
    </tr>
  </tbody>
</table>

<h2>支払方法<%= order.payment_method %> </h2>

<h2>お届け先 <%= order.address %></h2>
<%= form_with model: @order_new do |f| %>
<%= f.submit "注文を確定する" %>
